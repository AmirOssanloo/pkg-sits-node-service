{
  "name": "@sits/node-service",
  "version": "1.0.0",
  "repository": {
    "type": "git",
    "url": "git://github.com/amirossanloo/pkg-sits-node-service.git",
    "directory": "packages/configuration"
  },
  "publishConfig": {
    "registry": "https://npm.pkg.github.com/"
  },
  "type": "module",
  "exports": {
    ".": {
      "import": {
        "types": "./dist/esm/index.d.ts",
        "default": "./dist/esm/index.js"
      },
      "require": {
        "types": "./dist/cjs/index.d.ts",
        "default": "./dist/cjs/index.js"
      }
    }
  },
  "main": "./dist/cjs/index.js",
  "types": "./dist/cjs/index.d.ts",
  "files": [
    "dist"
  ],
  "scripts": {
    "build": "rimraf dist && pnpm build:esm && pnpm build:cjs && pnpm build:post",
    "build:esm": "tsc -p tsconfig.build.json",
    "build:cjs": "tsc -p tsconfig.build.cjs.json",
    "build:post": "cp -R src/types dist/esm/types && cp -R src/types dist/cjs/types && echo '{\"type\": \"commonjs\"}' > dist/cjs/package.json",
    "build:clean": "rimraf dist coverage *.tsbuildinfo",
    "build:watch": "concurrently \"pnpm build:esm --watch\" \"pnpm build:cjs --watch\"",
    "dev": "tsc --watch",
    "dev:esm": "tsc -p tsconfig.build.json --watch --incremental --tsBuildInfoFile .tsbuildinfo.esm.json",
    "dev:cjs": "tsc -p tsconfig.build.cjs.json --watch --incremental --tsBuildInfoFile .tsbuildinfo.cjs.json",
    "lint": "eslint src test --ext .ts",
    "lint:fix": "eslint src test --ext .ts --fix",
    "test": "NODE_OPTIONS='--experimental-vm-modules' jest",
    "test:watch": "NODE_OPTIONS='--experimental-vm-modules' jest --watch",
    "test:coverage": "NODE_OPTIONS='--experimental-vm-modules' jest --coverage",
    "test:coverage:badge": "pnpm test:coverage && node scripts/coverage-badge.js",
    "type:check": "tsc --noEmit",
    "validate": "pnpm lint && pnpm type:check && pnpm test",
    "validate:fix": "pnpm lint:fix && pnpm type:check && pnpm test",
    "prepack": "pnpm validate && pnpm build",
    "prepublishOnly": "pnpm test:coverage && pnpm build"
  },
  "peerDependencies": {
    "express": "^4.17.1 || ^5.0.0",
    "jsonwebtoken": "^9.0.1"
  },
  "dependencies": {
    "@sits/configuration": "workspace:*",
    "cookie-parser": "^1.4.7",
    "cors": "^2.8.5",
    "express": "^5.1.0",
    "helmet": "^8.1.0",
    "jsonwebtoken": "^9.0.2",
    "ramda": "^0.31.3",
    "uuid": "^11.1.0",
    "zod": "^4.0.10"
  },
  "devDependencies": {
    "@types/cookie-parser": "^1.4.9",
    "@types/cors": "^2.8.19",
    "@types/express": "^5.0.3",
    "@types/jest": "^29.5.14",
    "@types/jsonwebtoken": "^9.0.10",
    "@types/node-fetch": "^2.6.12",
    "@types/ramda": "^0.30.2",
    "@types/uuid": "^10.0.0",
    "concurrently": "^9.2.0",
    "fastify": "^5.4.0",
    "node-fetch": "2.6.7",
    "rimraf": "^6.0.1"
  }
}
